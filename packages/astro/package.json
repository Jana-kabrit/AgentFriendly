{
  "name": "@agentfriendly/astro",
  "version": "0.1.0",
  "description": "Astro integration for @agentfriendly — middleware + SSR support",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentfriendly/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "astro": "^4.16.19",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "astro": ">=4.0.0"
  },
  "keywords": [
    "astro",
    "ai",
    "agent",
    "middleware"
  ],
  "license": "MIT"
}
